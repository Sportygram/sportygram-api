test("adds 1 + 2 to equal 3", () => {
    expect(1 + 2).toBe(3);
});

test.todo("Happy path: Registering a new user registers a new user")
test.todo("Happy path: Registering a new admin registers a new admin")